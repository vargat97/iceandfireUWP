<Page
    x:Class="Iceandfire.Views.CharactersPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Iceandfire.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:vm="using:Iceandfire.ViewModels"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.DataContext>
        <vm:CharactersPageViewModel x:Name="CharactersPageVM"/>
    </Page.DataContext>

    <Grid>
        <ListView ItemsSource="{Binding NotifyCharacters.Result}"  ItemClick="ListView_ItemClick"  IsItemClickEnabled="True">
            <ListView.ItemTemplate>

                <DataTemplate>

                    <StackPanel Height="auto" Padding="5">
                        <TextBlock Text="{Binding name}"  Style="{ThemeResource BaseTextBlockStyle}"/>

                    </StackPanel>

                </DataTemplate>

            </ListView.ItemTemplate>

        </ListView>
    </Grid>
</Page>
